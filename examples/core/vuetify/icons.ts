/* eslint-disable camelcase */
import type { IconsOptions } from 'vuetify/types/services/icons'
import { mdiScriptText, mdiScriptTextOutline, mdiChevronDown, mdiCogOutline, mdiMenuDown, mdiTranslate } from '@mdi/js'

export const icons: IconsOptions = {
  iconfont: 'mdiSvg',
  values: {
    mdiScriptText,
    mdiScriptTextOutline,
    mdiChevronDown,
    mdiCogOutline,
    mdiMenuDown,
    mdiTranslate
  }
}
